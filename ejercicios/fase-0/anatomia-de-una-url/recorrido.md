# Mi recorrido — Anatomía de una URL

> Esqueleto del entregable. Reemplaza cada `<...>` con tu trabajo. **A mano, sin ejecutar, sin IA.**
> Borra esta cita cuando empieces.

## URL elegida

```
<pega aquí tu URL real y completa>
```

## 1. Tabla de descomposición

| Parte | Valor en mi URL | Qué hace |
|---|---|---|
| esquema (scheme) | `<...>` | `<...>` |
| host | `<...>` | `<...>` |
| puerto efectivo | `<...>` | `<por qué este puerto, aunque no esté escrito>` |
| ruta (path) | `<...>` | `<...>` |
| query string | `<...>` | `<...>` |
| fragmento | `<... o "no tiene">` | `<...>` |

## 2. Recorrido numerado (qué pasa al apretar Enter)

1. `<el navegador hace...>`
2. `<...>`
3. `<...>`
4. `<...>`
5. `<...>`
6. `<...>`
7. `<...>`

## 3. Qué parte de la URL NO viaja al servidor (y por qué)

`<tu respuesta>`

## 4. (Opcional) Una duda que me quedó

`<algo que no terminaste de entender — útil para la corrección>`
