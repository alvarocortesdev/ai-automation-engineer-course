#!/usr/bin/env bash
#
# consulta.sh — Primero-Sin-IA.
#
# Contrato:
#   entrada: la ruta de un JSON (lista de usuarios) por argumento ($1).
#   salida:  los nombres de los usuarios ACTIVOS con MÁS de 5 logins,
#            ordenados alfabéticamente, uno por línea, en crudo (sin comillas).
#
# Implementa el filtro jq a mano, sin IA. NO cambies el nombre del archivo:
# los tests (tests/test_consulta.py) invocan `bash consulta.sh <json>`.

set -euo pipefail

# TODO(estudiante): reemplaza estas dos líneas por tu solución con jq.
echo "Sin implementar: completa consulta.sh (ver README.md)" >&2
exit 2
