"""Parsons — ordena la función `promedio`.

Estas son las líneas CORRECTAS pero DESORDENADAS. Reordénalas (con su
indentación) reemplazando el stub de abajo. No agregues líneas nuevas ni
cambies nombres:

    total = 0
    def promedio(numeros):
        if not numeros:
            return 0.0
        for n in numeros:
            total = total + n
        return total / len(numeros)

Contrato:
    promedio([2, 4, 6]) -> 4.0
    promedio([])        -> 0.0   (no debe lanzar ZeroDivisionError)
"""


def promedio(numeros):
    # TODO: reemplaza este stub por las líneas reordenadas de arriba.
    raise NotImplementedError("Reordena las líneas del docstring aquí.")
