-- q2 — ORDER BY + LIMIT
-- Devuelve el `nombre` y el `precio` de los 3 productos más caros,
-- ordenados del más caro al más barato.
-- Columnas a devolver, en este orden: nombre, precio.
-- El orden de las filas SÍ se evalúa aquí.

-- TODO(estudiante): escribe tu consulta aquí (Primero-Sin-IA).
