-- q3 — GROUP BY + COUNT
-- Devuelve cuántos productos hay por categoría.
-- Columnas a devolver, en este orden: categoria, cantidad (el conteo).
-- El orden de las filas no se evalúa.

-- TODO(estudiante): escribe tu consulta aquí (Primero-Sin-IA).
