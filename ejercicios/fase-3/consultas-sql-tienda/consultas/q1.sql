-- q1 — SELECT con WHERE
-- Devuelve el `nombre` y el `stock` de los productos de la categoría 'periféricos'
-- que tengan stock mayor que 0.
-- Columnas a devolver, en este orden: nombre, stock.

-- TODO(estudiante): escribe tu consulta aquí (Primero-Sin-IA).
