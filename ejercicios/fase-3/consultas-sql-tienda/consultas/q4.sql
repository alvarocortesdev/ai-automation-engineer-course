-- q4 — GROUP BY + SUM
-- Devuelve la suma del `stock` por categoría.
-- Columnas a devolver, en este orden: categoria, suma_stock.
-- El orden de las filas no se evalúa.

-- TODO(estudiante): escribe tu consulta aquí (Primero-Sin-IA).
