# Copia este archivo a .env y rellena el valor real. El .env está en .gitignore: NUNCA se commitea.
# El token lo obtienes al crear el túnel en el dashboard de Cloudflare
# (Zero Trust -> Networks -> Tunnels). Es un secreto: trátalo como una contraseña.
CF_TUNNEL_TOKEN=
