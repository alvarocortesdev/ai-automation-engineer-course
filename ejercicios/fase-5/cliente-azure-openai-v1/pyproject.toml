[project]
name = "cliente-azure-openai-v1"
version = "0.1.0"
description = "Ejercicio 5.6 — cliente v1 de Azure OpenAI"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.55",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
