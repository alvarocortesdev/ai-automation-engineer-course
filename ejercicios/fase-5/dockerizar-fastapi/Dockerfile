# Tu Dockerfile de producción va aquí. Escríbelo a mano, sin IA.
#
# Requisitos (ver README.md):
#   1. Multi-stage: etapa `builder` (instala deps) + etapa `runtime` (mínima).
#   2. Base pinneada y slim (p. ej. python:3.13-slim). NADA de :latest.
#   3. Orden de capas: copia e instala requirements.txt ANTES de copiar ./app.
#   4. Usuario NO-root (crea un usuario de sistema y usa USER).
#   5. HEALTHCHECK que golpee /health SIN curl (la imagen slim no lo trae).
#   6. EXPOSE 8000 y CMD ["fastapi", "run", "app/main.py", "--port", "8000"].
#
# Borra estos comentarios cuando termines.
#
# TODO: escribe tu Dockerfile aquí.
