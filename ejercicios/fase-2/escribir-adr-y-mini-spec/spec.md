# Spec — acortar_titulo(titulo, max_len)

> Starter. Completa cada sección. Borra estas notas al entregar.
> Recuerda: el spec resuelve la ambigüedad del issue ANTES de codear. No implementes la función aquí.

## Objetivo
<!-- 1 línea: qué hace y para qué (la tarjeta de UI). -->

## Entradas
<!-- Tipos Y restricciones. Ejemplos de preguntas a responder:
     - titulo: str. ¿Puede ser vacío? ¿solo espacios?
     - max_len: int. ¿>= 0? ¿qué pasa con max_len muy chico (3, 1, 0)? ¿negativo? -->
- `titulo`:
- `max_len`:

## Salida
<!-- Tipo y la INVARIANTE que siempre se cumple (ej: len(resultado) <= max_len). -->
- Tipo:
- Invariante(s):

## Casos borde (acordados — resueltos a propósito, no "lo veré al codear")
<!-- Mínimo 3. Decide qué pasa en cada uno y por qué. -->
1. El título ya es más corto o igual que `max_len` →
2. `max_len` minúsculo (p. ej. 3) →
3. ¿El carácter de elipsis "…" cuenta dentro de `max_len` o se suma aparte? →
4. (opcional) El último espacio antes del corte queda muy lejos →

## Criterios de aceptación (falsables — cada uno se puede volver un test)
- [ ]
- [ ]
- [ ]
